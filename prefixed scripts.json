{
  "scripts": {
    "develop": "gatsby develop",
    "build": "gatsby build --prefix-paths && cp -r public/about public/about-test && rm public/component---src-pages-missing-resources*",
    "fullbuild": "gatsby-dev -sq && rm -rf public .cache && yarn build && yarn serve",
    "serve": "gatsby serve --prefix-paths"
  }
}
